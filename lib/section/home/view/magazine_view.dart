// magazine_view.dart
import 'package:flutter/material.dart';

class MagazineView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('매거진'),
      ),
      body: Center(
        child: Text('매거진 화면입니다.'),
      ),
    );
  }
}

