// place_detail_review_view.dart
import 'package:flutter/material.dart';

class PlaceDetailReviewView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('장소 리뷰보기'),
      ),
      body: Center(
        child: Text('장소 리뷰보기 화면입니다.'),
      ),
    );
  }
}

